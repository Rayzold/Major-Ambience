{
  "name": "major-ambience-sync-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: cloud-sync blob storage + magic-link auth for Major Ambience.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.5",
    "wrangler": "^3.99.0"
  }
}
