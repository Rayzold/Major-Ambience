// Placeholder kept so expo-router doesn't 404 on the path that used to
// exist in the scaffold. Hidden from the tab bar via href: null in
// _layout.tsx. Safe to delete once the scaffold gen-files are pruned.

export default function HiddenTwoScreen() {
  return null;
}
